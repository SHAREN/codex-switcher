{
  "name": "codex-switcher",
  "private": true,
  "version": "0.1.6",
  "description": "A multi-account manager for Codex CLI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:sync-order": "node --import tsx --test tests/otherAccountsOrder.test.ts tests/syncOrder.api.test.ts",
    "test:sync-order:ui": "node scripts/test-sync-order-ui.mjs",
    "tauri": "tauri",
    "lan": "pnpm build && cargo build --manifest-path src-tauri/Cargo.toml --bin codex-web && node scripts/lan.mjs",
    "lan:backend": "pnpm build && cargo run --manifest-path src-tauri/Cargo.toml --bin codex-web"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.0",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-process": "^2",
    "@tauri-apps/plugin-updater": "^2.10.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "playwright-core": "^1.58.2",
    "tailwindcss": "^4.1.18",
    "tsx": "^4.21.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
